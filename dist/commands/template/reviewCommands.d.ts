import { Command } from 'commander';
/** `template review add|list|rm`: 1-5 star reviews (only users who installed / used the template may write one). */
export declare function registerReviewCommands(tpl: Command): void;
//# sourceMappingURL=reviewCommands.d.ts.map