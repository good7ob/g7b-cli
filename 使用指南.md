# good7ob CLI 使用指南

> 当前活跃实现位于仓库根目录 `src/ -> dist/`。本文档以 **当前实际命令面** 为准。

---

## 1. 环境要求

- Node.js `>= 16`
- npm `>= 8`

---

## 2. 本地安装与运行

```bash
# 安装依赖
npm install

# 构建 CLI
npm run build

# 直接运行
node dist/index.js --help

# 可选：链接到全局命令
npm link
good7ob --help
```

> 说明：当前仓库尚未在公开 npm registry 验证发布，请优先使用本地构建 / `npm link` / Git 仓库方式使用，而不是假定 `npm install -g @good7ob/cli` 一定可用。

---

## 3. 配置

本地配置文件位置：

```text
~/.good7ob/config.json
```

默认值：

- `apiUrl`: `https://api.good7ob.net`
- `apiKey`: 空

常用命令：

```bash
# 设置 API 地址
good7ob config set api-url https://api.good7ob.net

# 设置 API Key
good7ob config set api-key g7b_sk_your_api_key_here

# 读取单个配置
good7ob config get api-url

# 查看全部配置（apiKey 会脱敏）
good7ob config list

# 清除已保存的 API Key
good7ob config clear-credentials

# 重置为默认配置
good7ob config reset
```

支持的配置键：

- `api-url`
- `api-key`
- `user-id`
- `theme`

---

## 4. 命令结构

```text
good7ob
├── config   # 本地 CLI 配置
├── infra    # 云基础设施、资源、成本、账单
├── pm       # 项目、任务、流程、报告、标签
├── org      # 组织、成员、邀请、产品、订阅
└── qc       # QA / Bug / 质量报表
```

查看帮助：

```bash
good7ob --help
good7ob config --help
good7ob infra --help
good7ob pm --help
good7ob org --help
good7ob qc --help
```

---

## 5. config 命令

```bash
good7ob config --help
```

子命令：

- `set <key> <value>`
- `get <key>`
- `list`
- `reset`
- `clear-credentials`

---

## 6. infra 命令

```bash
good7ob infra --help
good7ob infra app --help
good7ob infra resource --help
good7ob infra cost --help
good7ob infra bill --help
```

示例：

```bash
# 查看资源列表
good7ob infra resource list

# 查看单个资源
good7ob infra resource get i-1234567890
```

---

## 7. pm 命令

```bash
good7ob pm --help
good7ob pm project --help
good7ob pm task --help
good7ob pm workflow --help
good7ob pm report --help
good7ob pm tag --help
```

示例：

```bash
# 项目列表
good7ob pm project list

# 查看项目详情
good7ob pm project get 123

# 查看某项目下的任务
good7ob pm task list 123
```

---

## 8. org 命令

```bash
good7ob org --help
```

示例：

```bash
# 查看我的组织
good7ob org list

# 查看组织详情
good7ob org get 1001
```

---

## 9. qc 命令

```bash
good7ob qc --help
good7ob qc bug --help
good7ob qc report --help
```

适用于 QA / Bug 追踪与质量报表。

---

## 10. 构建与验证

```bash
npm run build
npm test
```

推荐最低验证步骤：

```bash
node dist/index.js --help
node dist/index.js config --help
node dist/index.js pm --help
node dist/index.js org --help
node dist/index.js qc --help
node dist/index.js infra --help
```

---

## 11. 已知历史遗留说明

仓库内仍保留了一些历史材料（例如 `node/` 子目录和部分 work-record 相关文档）。这些内容**不代表当前 CLI 的实际命令面**。如果说明与 `good7ob --help` 不一致，请以 `dist/index.js` 当前 help 输出为准。
